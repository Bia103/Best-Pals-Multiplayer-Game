using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using MLAPI;
using TMPro;
public class ButtonLeaveScript : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField]private Canvas leaveButton;
    public void Leave(){
       //PasswordNetworkManager.Leave();
    }
}
